Here some models are provided.

use

    cat x*> encdec.mdl

in the model directory to obtain the model file. 

All models use the bert\_base\_cased tokenizer as input. The output tokenizers are provided in tokenizers/dec directories.
